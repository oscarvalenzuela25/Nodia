import { styled } from "@mui/material/styles";

export const Wrapper = styled("div")({
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  width: "2.5rem",
  height: "2.5rem",
});

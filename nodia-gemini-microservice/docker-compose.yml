services:
  nodia-gemini-microservice:
    build: .
    container_name: nodia-gemini-microservice
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./browser_profile:/app/browser_profile
      - ./session_state:/app/session_state
      - ./.env:/app/.env
    environment:
      - PORT=8000
      - HOST=0.0.0.0
